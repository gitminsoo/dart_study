void main() {
  // list
  // collection for & collection if 지원

  var giveMeFive = true;

  var numbers = [
    1,
    2,
    3,
    4,
    if (giveMeFive) 5,
  ];
  // 마침표로 마무리 해주면 알아서 이케 한 열로 바뀜

  // if (giveMeFive) {
  //   numbers.add(5);
  // }
  // 요소에 if 넣어준거랑 같은 역할을 한다

  print(numbers);
}
