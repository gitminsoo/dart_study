void main() {
  var name = 'nico';
  var age = 10;
  var plus = 10;
  var greeting = 'Hello my name is ${name} and I\'m ${age + 2 + plus} years old';
  // 변수가 이미 있을 때 이렇게 $표시를 사용가능

  print(greeting);
}
